uint256 constant PRICE_GRID_FIXED_POINT = 1000100000000000000; // 1.0001

uint256 constant PUBLIC_SWAP_FEE = 60; // in bips?

uint256 constant EPOCH_LENGTH = 60; // in seconds
uint256 constant AUCTION_LENGTH = 60; // in seconds

address constant AUCTION_SETTLEMENT_TOKEN = address(0);

uint256 constant AUCTION_FEE = 60; // in bips?
